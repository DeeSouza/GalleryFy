import { styled } from "styled-components";

export const Container = styled.div`
  display: flex;
  position: relative;
  user-select: none;
  cursor: grab;
  align-items: center;
  justify-content: center;
`;
